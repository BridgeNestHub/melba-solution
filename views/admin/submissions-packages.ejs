<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body class="admin-body">
    <div class="admin-container">
        <%- include('../partials/admin-sidebar', { currentPage: 'admin-submissions' }) %>

        <main class="admin-main">
            <header class="admin-header">
                <div class="header-left">
                    <a href="/admin/submissions" class="back-btn">
                        <i class="fas fa-arrow-left"></i>
                    </a>
                    <h1>Package Quote Submissions</h1>
                </div>
                <div class="admin-user">
                    <i class="fas fa-user-circle"></i>Welcome, <%= adminUser %>
                </div>
            </header>

            <div class="submissions-container">
                <div class="submissions-header">
                    <div class="submissions-stats">
                        <div class="stat-item">
                            <span class="stat-number"><%= packages.length %></span>
                            <span class="stat-label">Total Packages</span>
                        </div>
                        <div class="stat-item">
                            <span class="stat-number"><%= packages.filter(p => p.status === 'new').length %></span>
                            <span class="stat-label">New</span>
                        </div>
                        <div class="stat-item">
                            <span class="stat-number"><%= packages.filter(p => p.status === 'contacted').length %></span>
                            <span class="stat-label">Contacted</span>
                        </div>
                    </div>
                </div>

                <div class="submissions-grid">
                    <% packages.forEach(pkg => { %>
                        <div class="submission-card">
                            <div class="submission-header">
                                <div class="submission-avatar">
                                    <i class="fas fa-box"></i>
                                </div>
                                <div class="submission-basic">
                                    <h3><%= pkg.name %></h3>
                                    <p class="submission-email"><%= pkg.email %></p>
                                </div>
                                <div class="submission-status">
                                    <span class="status status-<%= pkg.status %>"><%= pkg.status.replace('-', ' ') %></span>
                                </div>
                            </div>
                            
                            <div class="submission-details">
                                <div class="detail-row">
                                    <i class="fas fa-phone"></i>
                                    <span><%= pkg.phone || 'Not provided' %></span>
                                </div>
                                <div class="detail-row">
                                    <i class="fas fa-building"></i>
                                    <span><%= pkg.company || 'Not provided' %></span>
                                </div>
                                <div class="detail-row">
                                    <i class="fas fa-box"></i>
                                    <span><%= pkg.packageType || 'Package Quote' %></span>
                                </div>
                                <div class="detail-row">
                                    <i class="fas fa-dollar-sign"></i>
                                    <span><%= pkg.price || pkg.budget || 'Not specified' %></span>
                                </div>
                                <div class="detail-row">
                                    <i class="fas fa-clock"></i>
                                    <span><%= pkg.timeline || 'Not specified' %></span>
                                </div>
                                <div class="detail-row">
                                    <i class="fas fa-calendar"></i>
                                    <span><%= new Date(pkg.date).toLocaleDateString() %></span>
                                </div>
                            </div>
                            
                            <div class="submission-message">
                                <h4>Project Description:</h4>
                                <p><%= pkg.message %></p>
                            </div>
                            
                            <div class="submission-actions">
                                <button class="btn-primary" onclick="updateStatus('<%= pkg.id %>', 'contacted')">
                                    <i class="fas fa-phone"></i> Schedule Call
                                </button>
                                <button class="btn-secondary" onclick="updateStatus('<%= pkg.id %>', 'proposal-sent')">
                                    <i class="fas fa-calendar-plus"></i> Book Meeting
                                </button>
                                <button class="btn-success" onclick="updateStatus('<%= pkg.id %>', 'completed')">
                                    <i class="fas fa-check"></i> Complete
                                </button>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
        </main>
    </div>

    <%- include('../partials/admin-styles') %>
    <script src="/js/admin.js"></script>
</body>
</html>